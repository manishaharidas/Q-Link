<!DOCTYPE html>
<html>

  <link href="/assets/profiles.css" media="screen" rel="stylesheet" type="text/css" />		
			
				<div class="span7">  
            		<ul class="nav nav-tabs" >
    					<li><%= link_to 'About', about_profiles_path, :style=>"padding: 10px 30px;"%></li>    			
    					<li><%= link_to 'Posts', about_profiles_path, :style=>"padding: 10px 30px;"%></li>
    					<li><%= link_to 'Photos', about_profiles_path, :style=>"padding: 10px 30px;"%></li>
    					<li><%= link_to 'Videos', about_profiles_path, :style=>"padding: 10px 30px;"%></li>
    					<li><%= link_to 'Events', about_profiles_path, :style=>"padding: 10px 30px;"%></li>
    				</ul>   
    			</div>
    			<div id="profile_form">
					<h2 id="heading">Profile Updation</h2>
    				<div id="form_fields">
    					
    						<table cellpadding="10px" >
    							<%=  form_for :profile, :url => {:controller => 'profiles', :action => 'create'} do |f| %>
   									<tr>
   										<td><%= f.label :first_name%></td>
   										<td><%= f.text_field :first_name, :autofocus => true %></td>
   									</tr>

   									<tr>
   										<td><%= f.label :last_name%></td>
   										<td><%= f.text_field :last_name , :autofocus => true%></td>
   									</tr>

   									<tr>
   										<td><%= f.label :date_of_birth%></td>
   										<td><%= f.text_field :date_of_birth, 'data-behaviour' => 'datepicker' %></td>
   									</tr>

   									<tr>
   										<td><%= f.label :place%></td>
   										<td><%= f.text_field :place, :autofocus => true %></td>
   									</tr>

  									<tr>
  										<td></td>
  										<td id="submit"><%= f.submit "Update", 'class'=>"btn btn-primary",'data-loading-text'=>"Updating..."%></td>
  									</tr>
   								<% end %>
    						</table>  
    					
    				</div> 
		 		</div> 
			
</html>